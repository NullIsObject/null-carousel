{
  "name": "null-carousel",
  "version": "0.0.0",
  "description": "A Carousel Component Library for Vue.js.",
  "packageManager": "pnpm@8.14.1",
  "type": "module",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "pnpm run -C build build:packages",
    "orderlyBuild": "pnpm run -C build build:packages_orderlyBuild",
    "dev": "pnpm run -C packages/dev start",
    "build_watch": "pnpm run -C build build:packages_watch_build"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/NullIsObject/null-carousel.git"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "keywords": [
    "carousel",
    "vue",
    "components"
  ],
  "devDependencies": {
    "@null-carousel/build": "workspace:*",
    "@null-carousel/root": "link:",
    "null-carousel": "workspace:*",
    "typescript": "^5.5.4",
    "vue": "^3.4.23"
  }
}
